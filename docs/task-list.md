# 当前研发任务列表

更新时间：2026-05-21

## 使用规则

- 未完成：`[ ]`
- 已完成：`[x]`
- 每完成一条研发任务，就把对应项改成 `[x]`
- 只记录当前阶段要做的事，不记录长期愿望清单

## 当前阶段目标

把 MVP 从“能展示 seed 数据”推进到“可录入、可评估、可解读、可维护”的第一版可用产品。

## 本轮新增：测试数据与 DeepAgent

- [x] 构造测试数据场景，覆盖弱信用、内生修复、成本推动压力。
- [x] 增加测试数据场景 API，可一键应用到数据库并重新计算规则。
- [x] 安装并接入 `deepagents` 和 `langchain-openai`。
- [x] 开发 Macro Cycle Agent：基于 DeepAgent runtime，提供指标、规则、快照、执行日志 4 个工具。
- [x] 增加 DeepAgent 调用能力，支持有密钥时真实调用、无密钥时 mock 返回。
- [x] 增加 Agent 状态 API，前端可查看 runtime、模型配置和工具列表。
- [x] 增加 Agent 工作台 `/agent`，支持应用测试数据、选择月份、触发解读、展示结果。
- [x] 更新 README 和项目状态文档，说明模型环境变量和使用入口。

## P0：打通核心闭环

- [x] 新增 Agent 解读 API：输入月份，读取指标数据、规则命中和周期快照，先返回 mock 解读结果。
- [x] 将 `agent_interpretation_prompt.md` 接入后端服务，提供统一的 prompt 加载函数。
- [x] 优化规则命中证据文本：根据 `evidence_template` 生成用户可读的命中依据。
- [x] 前端首页接入 Agent 解读 API，用真实接口内容替换当前占位文案。
- [x] 增加月份选择能力：用户可以切换不同月份查看指标、规则和快照。
- [x] 修正规则结果与快照模块状态的映射，让 `policy`、`cycle` 等规则不会丢失展示。

完成说明：P0 已实现后端 Agent mock 解读、prompt 加载、规则证据文本渲染、月份列表接口、首页月份切换和跨模块规则信号展示。

## P1：数据录入与维护

- [x] 新增指标数据录入页面，支持单条手动录入。
- [x] 新增 CSV 上传页面，调用现有 CSV 导入接口。
- [x] 增加 CSV 模板下载，字段与后端导入格式保持一致。
- [x] 导入数据后自动重新计算该月份规则结果和周期快照。
- [x] 增加基础数据校验：月份格式、数值字段、指标 ID 是否存在。
- [x] 指标定义页面支持查看 12 个指标的定义、解释、风险提示和相关指标。

完成说明：P1 已新增 `/data` 数据维护页、`/indicators` 指标定义页、CSV 模板接口、导入校验和导入后自动重算。

## P2：规则系统增强

- [x] 为规则 seed 增加版本号和更新时间字段。
- [x] 增加规则列表 API，返回规则说明、观察指标和触发状态。
- [x] 前端增加规则说明视图，展示每条规则的条件和解释。
- [x] 增加规则执行日志，记录某月每条规则为何命中或未命中。
- [x] 为规则条件增加字段白名单，避免 YAML 配置写错字段后静默失败。

完成说明：P2 已新增规则元信息、规则加载校验、规则执行日志和 `/rules` 规则说明页。

## P3：工程质量

- [x] 引入 Alembic 管理数据库迁移，替代启动时直接 `create_all`。
- [x] 增加后端单元测试：seed 读取、规则加载、规则评估、dashboard 聚合。
- [x] 增加前端基础测试或类型检查脚本，确保页面和 API 类型不漂移。
- [ ] 整理 Docker 启动流程，确认全新环境下 `docker compose up --build` 可用。
- [x] 清理开发产物，确保 `.next`、`node_modules`、`__pycache__` 不进入版本管理。

进展说明：已加入 Alembic 初始迁移、后端 pytest 覆盖 seed/规则/dashboard/Agent mock、前端 `typecheck` 脚本，并清理生成物；Docker 全新启动验证因当前机器缺少 `docker` 命令暂未完成。

## P4：产品表达优化

- [x] 优化首页信息层级，让“本月判断 -> 模块状态 -> 证据 -> 风险”更清晰。
- [x] 趋势图支持选择指标，而不是固定展示少数指标。
- [x] 指标表增加详情展开，显示定义、解释和风险提示。
- [x] 增加“下月观察任务”生成逻辑，不再只使用固定文本。
- [x] 增加空数据和数据不足状态提示。
- [x] 将首页重构为 ChatGPT 式 AI 工作台：左侧资产栏、中间 Agent 对话、右侧周期数据看板。
- [x] 右侧看板降低复杂度：只保留当前周期状态卡片、全部指标趋势图、历史对比与来源表。
- [x] Agent 输出改为流式 Markdown 渲染，避免在聊天里重复右侧已有的数据表。
- [x] 状态卡片支持点击后带入 Agent 解释问题。

完成说明：P4 已优化首页入口和证据展示，趋势图支持指标切换，并进一步演进为 AI 工作台。右侧用于持续展示图、表、来源和周期状态；中间 Agent 专注回答、解释和生成。

## P5：真实数据与可验证来源

- [x] 增加最新公开官方快照同步入口。
- [x] 在指标来源中保留真实链接，前端可点击跳转。
- [x] 指标表突出历史对比和来源验证。
- [ ] 继续扩展按指标自动抓取或半自动更新适配器。

## 暂不做

- [ ] 不接复杂爬虫。
- [ ] 不做实时行情。
- [ ] 不做具体投资推荐。
- [ ] 不做用户权限系统。
- [ ] 不做复杂机器学习模型。
